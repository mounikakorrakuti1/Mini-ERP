import { PlaceholderPage } from '@/components/shared/PlaceholderPage';
import { Factory } from 'lucide-react';

export default function ManufacturingOrderDetailPage() {
  return (
    <PlaceholderPage
      title="Manufacturing Order Detail"
      icon={Factory}
      moduleName="Manufacturing"
      description="View manufacturing order details with component consumption tracking, work order management, status-aware actions (Confirm, Start, Done, Cancel), and audit trail."
    />
  );
}
