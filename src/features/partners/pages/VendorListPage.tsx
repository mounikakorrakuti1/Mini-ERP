import { PlaceholderPage } from '@/components/shared/PlaceholderPage';
import { Building2 } from 'lucide-react';

export default function VendorListPage() {
  return (
    <PlaceholderPage
      title="Vendors"
      icon={Building2}
      moduleName="Partners"
      description="Manage vendor profiles, addresses, and view their purchase order history."
    />
  );
}
